const { instanceVisaAPI, getXPayToken } = require('../utils');
const { methods, routers, routersForToken } = require('../enum');

async function pullFundsTransactions({ body }) {
  const xPayToken = getXPayToken({
    body,
    resourcePath: routersForToken.PULL_FUNDS_TRANSACTIONS,
  });

  const response = await instanceVisaAPI.request({
    method: methods.post,
    url: routers.PULL_FUNDS_TRANSACTIONS,
    data: body,
    headers: {
      'x-pay-token': xPayToken,
    },
  });

  return response;
}

module.exports = { pullFundsTransactions };
